#!/bin/bash

CONTAINER_NAME="qwen3-4b"
IMAGE_NAME="qwen3-4b-transformers"
PORT="8100"
DOCKERFILE="Dockerfile.qwen3_4b"

case "${1:-help}" in
  build)
    echo "Building Docker image..."
    DOCKER_BUILDKIT=0 docker build -f $DOCKERFILE -t $IMAGE_NAME .
    ;;
  up)
    if docker ps -a --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
      echo "Starting existing container..."
      docker start $CONTAINER_NAME
      echo "Container $CONTAINER_NAME started on port $PORT"
    else
      echo "Creating and starting new container..."
      docker run -d --name $CONTAINER_NAME --gpus all -p $PORT:$PORT -v /mnt/data:/mnt/data $IMAGE_NAME
      echo "Container $CONTAINER_NAME created and started on port $PORT"
    fi
    ;;
  down)
    if docker ps -a --format '{{.Names}}' | grep -q "^${CONTAINER_NAME}$"; then
      docker stop $CONTAINER_NAME
      echo "$CONTAINER_NAME"
    else
      echo "Container $CONTAINER_NAME is not running"
    fi
    ;;
  restart)
    $0 down
    sleep 2
    $0 up
    ;;
  logs)
    docker logs $CONTAINER_NAME
    ;;
  status)
    docker ps -a --filter "name=$CONTAINER_NAME"
    ;;
  health)
    response=$(curl -s http://localhost:$PORT/health 2>&1)
    if echo "$response" | grep -q "ok"; then
      echo "✓ Qwen3-4B API is responding"
      exit 0
    else
      echo "✗ Qwen3-4B API is not responding"
      exit 1
    fi
    ;;
  *)
    echo "Usage: $0 {build|up|down|restart|logs|status|health}"
    exit 1
    ;;
esac
