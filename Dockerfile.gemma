FROM python:3.10-slim

WORKDIR /app

RUN pip install --no-cache-dir torch torchvision transformers fastapi uvicorn pillow accelerate compressed-tensors

COPY gemma_server.py .

EXPOSE 8010

CMD ["python", "gemma_server.py"]
